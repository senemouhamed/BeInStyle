<style>
form{
    padding: 0px 3px;
  font-weight: bold;
  font-variant: small-caps;
  
}
</style>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enrégistrer') }}</button>
{{ form_end(form) }}